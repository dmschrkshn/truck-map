# Проект: интерактивная карта автовозов для клиентов

Документ для передачи контекста в новый чат. Описывает что построено,
как устроено, все настройки и открытые вопросы.

---

## 1. Задача

Компания импортирует авто из Китая (воронка «Китай» в Битрикс24).
Водители автовозов присылают геолокацию в WeChat ~2 раза в неделю.
Нужна интерактивная карта для клиентов, где видно все автовозы в пути,
без ручного перетаскивания иконок.

Раньше метки двигали вручную в Конструкторе Яндекс.Карт. Ушли от этого.

---

## 2. Итоговая архитектура

```
Битрикс24 (воронка Китай, сделки = машины)
   │  вебхук, раз в 6 ч (dailyRefresh)
   ▼
Google Sheets, лист «Автовозы» ← актуальный список автовозов в пути
   │
   │  вы вручную вставляете локацию из WeChat в лист «Журнал»
   ▼
Apps Script: парсит ссылку → координаты, считает расстояние/ETA/пробег
   │
   ├─→ лист «Экспорт» → doGet (веб-приложение) → JSON
   │        │
   │        ▼
   │   index.html на GitHub Pages (карта Яндекс JS API)
   │        │
   │        ▼
   │   iframe в Сайты24 (Битрикс24) → клиент
   │
   └─→ лист «Панель» — внутренний дашборд для сотрудника
```

---

## 3. Google-таблица: листы

**Журнал** — сюда вносятся локации вручную:
- A: Дата (авто)
- B: Автовоз (выпадающий список из листа «Автовозы»)
- C: Ссылка / координаты (вставляете сюда)
- D: lat (авто), E: lon (авто)
- F: (не используется)
- G: Комментарий (виден клиенту в балуне)
- H: Прежний номер (для перегрузки на другой автовоз)

**Автовозы** — заполняется автоматически из Битрикса:
- строка 1: отметка времени синхронизации
- строка 2: заголовки
- A: Автовоз, B: Активен, C: Место ТО, D: Скрыт с карты,
  E: На СВХ / в партии, F: Статус

**Экспорт** — технический, читается картой (не трогать руками).

**Панель** — дашборд: статусы, расстояния, кто требует внимания.

---

## 4. Ключевые настройки в начале Код.gs

```
MSK               — координаты Москвы (точка отсчёта расстояния)
AVG_SPEED_KMH=55  — средняя скорость автовоза для ETA
HOURS_PER_DAY=9   — рабочих часов в сутки
ROAD_FACTOR=1.25  — коэффициент «дороги длиннее прямой»
MIN_KM_PER_DAY=150— меньше → статус «стоит»
MAX_AGE_DAYS=14   — локация старше → уходит с карты

B24_CATEGORY  = 4                      — воронка Китай
B24_TRUCK     = UF_CRM_1755854996126   — госномер автовоза
B24_ARRIVED   = UF_CRM_1734620131      — дата прибытия на СВХ
B24_TO_PLACE  = UF_CRM_1722393583543   — место ТО (справочник)
B24_SHIP_DATE = UF_CRM_1776043454137   — дата отправки
B24_BATCH_DAYS=2                       — окно партии, ±дней от свежей отправки
B24_ARRIVING_LEFT=2                    — осталось ≤N машин → «Прибывает»
B24_HIDE_TO = ['Чита','Хоргос-Москва'] — места ТО, скрытые от клиента
B24_STAGES  = ранние стадии (Транзит, Брокерский договор, Предв. пошлина)
```

### Вебхук Битрикса — в коде его нет

Репозиторий публичный, поэтому вебхук хранится не в `Код.gs`, а в свойствах
скрипта (`PropertiesService`, ключ `B24_WEBHOOK`). Код обращается к нему через
функцию `b24()`; если значение не задано — понятная ошибка, а не молчаливый сбой.

Задать: меню **🚚 Автовозы → Вебхук Битрикса** (или Настройки проекта →
Свойства скрипта). Проверить: **🚚 Автовозы → Проверить связь с Битриксом**.
Свойства скрипта переживают полную замену кода — при обновлении `Код.gs`
задавать заново не нужно.

### Коды стадий воронки Китай (CATEGORY_ID=4)
```
C4:UC_P1CA59  Транзит                ← ранняя (едет)
C4:UC_9HAG50  Брокерский договор     ← ранняя (едет)
C4:UC_OWAJBE  Предварительная пошлина← ранняя (едет)
C4:PREPAYMENT_INVOICE  Автомобиль на СВХ
... далее оформительные, всё = «прибыл»
```

### Справочник места ТО (UF_CRM_1722393583543)
```
56 Уссурийск · 2090 Уссурийск-Москва · 74 Чита · 2088 Чита-Москва
2162 Хоргос-Москва · 1006 Владивосток · 58 Благовещенск · 1956 Беларусь
```

---

## 5. Логика, до которой дошли (важные решения)

**Список автовозов** ведётся из Битрикса, не вручную. Автовоз = уникальный
госномер из сделок воронки Китай.

**Партия по дате отправки.** Автовоз возит партии по кругу. Текущая партия =
машины, отправленные в пределах ±2 дней от самой свежей даты отправки.
Прошлые партии (старые даты) не учитываются. Это чинит проблему «1/2 вместо 1/9».

**Прибыла машина, если:** заполнена дата СВХ ИЛИ стадия не из трёх ранних.
Дата СВХ приоритетнее стадии (менеджер мог не переключить стадию).

**Автовоз «в пути»:** в текущей партии есть машины на ранних стадиях без даты СВХ.

**Статус «Прибывает»:** осталось ≤2 машины в пути. Виден в панели (голубой),
НЕ на карте, НЕ просит обновления.

**Статус «Нет данных»:** автовоз в списке, но локации нет / старше 14 дней.
Красный, вверху панели — сигнал запросить локацию в WeChat.

**Скрытие с карты:** по месту ТО (Чита, Хоргос-Москва сейчас скрыты) —
автоматически. Или вручную колонкой D в листе «Автовозы» (но синхронизация
перезапишет — для постоянного скрытия только через B24_HIDE_TO).

**Смена номера (перегрузка):** новая строка в Журнале, новый номер в B,
старый в H. Клиент видит «蒙E50583 (ранее 吉ARM361)».

**Перегруз в Забайкальске (номер через тире).** Номер вида
`Н625ХТ75-М118УК13`: первая машина везёт до Забайкальска, там перегруз, дальше
до Москвы едет вторая. В таблице ничего не меняется — в Журнале выбирается
полный номер, история копится под ним. Разбирает только карта (`relayInfo()`
в index.html):
- в радиусе 40 км от Забайкальска → фаза «перегруз», на метке первый номер,
  в списке и подсказке пометка «перегруз»;
- южнее или восточнее (Китай) → «до Забайкальска», на метке первый номер;
- иначе (Россия, западнее) → «после», на метке второй номер.
В балуне — цепочка «первый → второй», текущая машина жирным. Поиск находит по
любой половине. Разделители `-`, `–`, `—`; обе части не короче 5 символов,
иначе номер считается обычным.

**Цвета на карте по давности локации:**
- зелёный ≤2 дня · жёлтый 3–4 · чёрный 5+ · синий = «стоит» (малое перемещение)

---

## 6. Веб-часть

**index.html** — Яндекс JS API. Возможности:
- метка на карте — белая «таблетка» с хвостиком: слева цветной блок со светлым
  автовозом, справа номер тёмным шрифтом. Кончик хвостика — точная точка
  локации. Цвет блока и обводки — по давности; бейдж справа сверху: красная
  точка = есть комментарий, синий «!» = стоит
- сам автовоз (`carrierShape()`, viewBox 60×31) — тягач кабиной влево (едут
  на запад, к Москве), верхний ярус над кабиной, 4 машины сверху и 2 снизу,
  фара, две оси прицепа. Одна функция рисует и метку, и иконку в списке
- ширина метки считается по номеру (`textWidth()`): иероглифы шире латиницы,
  поэтому «蒙E50583» не обрезается

**Телефон (ширина до 720 px, `isMobile`).** Карта на сайте LikeAvto встроена
в iframe, и на телефоне это маленькое окошко — около трети экрана. Поэтому:
- список — на весь экран со своим крестиком; пока он открыт, кнопка «Поиск»
  и легенда скрыты (раньше «Поиск» налезал на заголовок, легенда — на список);
- легенда свёрнута в кнопку «Состояние ▾» справа сверху;
- из кнопок Яндекса только компактный масштаб справа; «Слои», «На весь экран»
  и «Открыть в Картах» скрыты;
- кластер открывается компактным списком (`cluster#balloonAccordion`), а не
  каруселью с листанием — карусель в окошко не помещалась; ширина и высота
  балунов считаются от размера окна;
- `fitToTrucks()` перед подгонкой пересчитывает размер контейнера — без этого
  внутри iframe карта открывалась масштабом «весь мир»; повторяется при
  повороте телефона, пока человек сам не двигал карту.

Режим «балун панелью снизу» (`balloonPanelMaxMapArea`) пробовали и убрали:
панель открывалась с нулевой высотой, то есть невидимой.

Если окошко на сайте всё равно тесное — высота iframe задаётся в редакторе
страницы Сайтов24, 70–80% высоты экрана телефона будет заметно удобнее.
- боковая панель со списком + поиск по номеру
- на мобильном панель свёрнута, кнопка «🔍 Поиск», закрывается после выбора
- балун: до Москвы, ETA, прошёл с прошлого раза, обновлено, комментарий, прежний номер
- кластеры при скоплении
- автообновление раз в 5 мин

Ключ Яндекса (в index.html): `aeb19683-8505-416a-a1c4-090e24030cd0`
API карты (в index.html): веб-приложение Apps Script `.../exec`
GitHub Pages: `https://dmschrkshn.github.io/truck-map/`
Домены в ключе Яндекса (HTTP Referer): dmschrkshn.github.io, likeavto.bitrix24site.ru

---

## 7. Меню в таблице (🚚 Автовозы)

Меню строит `onOpen()`, оно появляется правее пункта «Справка» после перезагрузки
вкладки с таблицей.

- **Обработать все ссылки** — парсит координаты для всех строк Журнала разом.
  Решает проблему вставки с ПК / блоком, когда триггер handleEdit не срабатывает.
  Строки, где lat и lon уже числа, пропускает; строки с «ОШИБКА» пробует заново.
- **Обновить панель и карту** — пересобрать Экспорт и Панель.
- **Синхронизировать из Битрикса** — обновить список автовозов вручную.
- **Вебхук Битрикса** — задать входящий вебхук (хранится в свойствах скрипта).
- **Проверить связь с Битриксом** — дёргает `profile.json`, показывает,
  от чьего имени работает вебхук.

---

## 8. Триггеры Apps Script

Должно быть два:
1. `handleEdit` — Из таблицы · При редактировании (одиночная вставка с телефона)
2. `dailyRefresh` — По времени · Каждые 6 часов (синхронизация + пересборка)

---

## 9. Рабочий процесс сотрудника

1. Пришла локация в WeChat
2. Журнал → новая строка → выбрать автовоз в B → вставить ссылку в C
3. С телефона: координаты появятся сами. С ПК/блоком: меню → «Обработать все ссылки»
4. Панель показывает, кого ещё не обновили (красный «Нет данных»)
5. Карта обновляется сама, клиент видит актуальную картину

---

## 10. Открытые вопросы / что можно улучшить

- ETA считается по прямой ×1.25, грубо. Точнее — через Яндекс.Маршрутизатор.
- Трек (линия пройденного пути) — обсуждали, не делали.
- Поворот иконки по направлению движения — не делали.
- Смена номера сейчас фиксируется вручную (колонка H). Из Битрикса
  автоматически не тянется — там нет истории номеров.

---

## 11. История багов (чтобы не наступить снова)

- **Даты вместо X/Y в колонке партии:** Google Sheets принимал «8/9» за
  «8 сентября». Решение: колонка партии форматируется как текст (setNumberFormat '@').
- **Неверная последняя дата (07.08 вместо 11.08):** dayKey давал ключи без
  ведущих нулей (2026-8-7), сортировались как текст неправильно. Решение:
  ведущие нули (2026-08-07).
- **Координаты с запятой:** toNum() приводит «55,13» → 55.13.
- **Мусорные номера (даты, «-»):** looksLikeTruck() отсекает.
- **isEmpty is not defined:** при частичной вставке кода. Всегда вставлять
  файл целиком (Ctrl+A → Delete → вставить).
- **Ссылка с ПК «не работает»:** на деле парсится, проблема была в триггере.
  Решение — меню «Обработать все ссылки».
- **Меню пропало из кода:** в `Код.gs` не было ни `onOpen()`, ни
  `processAllLinks()`, хотя документация их описывала. Не сработал триггер —
  и запасного пути не оставалось. Восстановлено.
- **Ссылка-чип вместо текста:** Sheets превращает вставленный URL в
  гиперссылку с подписью, и `getValue()` возвращает подпись, а не адрес.
  Решение — `readLinkCell()`: читает `RichTextValue.getLinkUrl()`, формулу
  `HYPERLINK` и только потом текст, пробует все варианты по очереди.
- **Перепутанные широта и долгота:** у Яндекса в `ll=` и `whatshere[point]=`
  порядок «долгота, широта», у Google в `@` — наоборот. Разбирали всё как
  Google, метка уезжала. Плюс шаблон `\d{1,2}` не совпадал с трёхзначной
  долготой, и для Читы/Владивостока запасной вариант вытаскивал обрезок
  («131.88» → «31.88»). Решение — свои правила на каждый сервис + `sane()`,
  которая ловит перевёрнутую пару.
- **Короткие ссылки:** разворачивались только `goo.gl`. Яндекс `maps/-/CD…`
  и 2ГИС не разворачивались вообще.
- **maps.app.goo.gl не раскрывалась:** из браузера Google отдаёт `302` с
  заголовком `Location`, а серверу Apps Script — `200` и HTML-заглушку.
  Настоящий адрес там `/maps/search/51.35,+110.39`: координаты в пути URL,
  а не в `q=`, и разделены `,+`. Решение — `expandUrl()` читает тело
  (meta refresh → адрес `/maps/search|place|dir/` → `ll=`/`whatshere`/`/@`),
  а при неудаче координаты ищутся прямо в содержимом страницы.
- **`decodeURIComponent` без try:** одиночный `%` в ссылке ронял `handleEdit`
  целиком, и строка молча оставалась пустой. Решение — `safeDecode()`.
- **Вставка нескольких строк разом:** обрабатывалась только первая, потому что
  брался `e.range.getColumn()` без учёта размера диапазона.

- **0 из N хотя прибыли:** прошлые партии отсекались, текущая реально ещё едет.
  Оказалось корректно после разбора логики партий.

- **dailyRefresh падал в 18% запусков** (5 из 28 за неделю до 12.09.2026):
  4 раза «You do not have permission to access the requested document» на
  `sh.setFrozenRows(2)` в `syncTrucksFromB24` — Google иногда отказывает в этой
  косметической операции посреди длинного запуска; 1 раз — лимит 6 минут.
  Запуск шёл 110–265 с, потому что каждые 6 часов выкачивались все ~7 200
  сделок воронки (145 страниц). Решение: `freezeRows()` — закрепляет, только
  если не закреплено, ошибку пишет в журнал и не роняет запуск; выгрузка —
  только сделки с отправкой за `B24_SYNC_DAYS` = 120 дней (~31 страница;
  проверено на всей воронке — список «в пути» тот же, 0 расхождений);
  `b24Post()` повторяет запрос при временном сбое Битрикса; `dailyRefresh`
  пересобирает Экспорт и Панель даже при сбое синхронизации.
- **Автовозы с Хоргоса не скрывались** (найдено 15.09.2026): в `B24_HIDE_TO`
  было «Харгос-Москва», а в справочнике Битрикса — «Хоргос-Москва» (ID 2162),
  сравнение шло буква в букву. Опечатка была и в этом документе. Решение —
  правильное название + `isHiddenPlace()` / `placeKey()`: без учёта регистра,
  пробелов и вида тире. Скрытие пересчитывается при синхронизации с Битриксом.
- **Как смотреть ошибки триггеров:** Apps Script → «Количество выполнений» →
  «Добавить фильтр» → «Тип» → «Триггер по времени»; упавшую строку раскрыть —
  там текст ошибки и строка кода.

При неудачном разборе скрипт пишет примечание в ячейку C: что именно он
проверял и во что раскрылась ссылка. Это первое, куда смотреть.

---

## 12. Сроки прибытия по истории

Раньше срок считался формулой «55 км/ч × 9 ч в сутки» и в среднем ошибался на
3,2 дня — почти всегда в большую сторону: реально автовозы идут ~686 км/сутки
с учётом стоянок. Клиенты злились, что сроки не совпадают.

Теперь срок — коридор «обычно … до» (медиана и 80% прошлых рейсов с похожего
расстояния). Разметка: для каждой отметки Журнала берётся ближайшая дата
прибытия на СВХ этого автовоза из Битрикса (`UF_CRM_1734620131`, окно −1…+40
дней), «осталось дней» = СВХ − отметка. Узлы 500…6400 км, окно ±700 км,
монотонно (дальше — не быстрее). Забайкальск (40 км) — отдельный коридор: там
весь разброс — ожидание на границе (от 7 до 30 дней).

Проверка на автовозах, которых модель не видела: ошибка 1,7 дня против 3,2 у
формулы, коридор ~2 дня, позже верхней границы ~10% (4–15% по половинам).

Стартовая таблица (12.09.2026, 256 отметок) зашита в `ETA_SEED`, текущая —
в свойствах скрипта `ETA_MODEL`. Меню: «Пересчитать сроки по истории»
(`retrainEta`) и «Включить еженедельный пересчёт сроков» (понедельник, ~6 утра).
Если размеченных отметок меньше 80 — таблица не трогается. Чистая функция
`fitEtaModel()` проверяется в Node на выгрузке Журнала и Битрикса.

В Экспорт добавлены колонки 11–13: «Срок от», «Срок до», «Трек». ETA (кол. 6)
= верхняя граница, чтобы старая карта не ломалась. Панель показывает «5–6».

## 13. Треки (пройденный путь)

`getHistoryByTruck()` отдаёт `track` — отметки текущего рейса (по одной в
день): от последней назад, пока нет разрыва >10 дней и пока не упёрлись в
Москву перед дальней точкой. Карта рисует линию только у выбранного автовоза
(балун, балун кластера, выбор в списке) и убирает при закрытии. Линия прямая
между отметками, не по дорогам.

**После правки `doGet` в Apps Script обязательно: Развертывание → Управление
развертываниями → карандаш → Версия: «Новая версия» → Развернуть.** Иначе
веб-приложение продолжает отдавать старый код.

## 14. Поддерживаемые форматы ссылок

- голые координаты: `55.7558, 37.6173`, `55,7558 37,6173`, `55.7558;37.6173`
- ссылка внутри текста: «Вот я: https://… еду»
- Яндекс: `ll=`, `pt=`, `whatshere[point]=`, `rtext=`, короткие `maps/-/CD…`
- Google: `/@lat,lon`, `!3d…!4d…`, `?q=`, `/maps/search|place|dir/lat,+lon`,
  короткие `maps.app.goo.gl`
- 2ГИС: `?m=lon,lat`, `/geo/…/lon,lat`, короткие `go.2gis.com`
- Apple Maps, Amap, произвольные `?lat=…&lon=…`

Порядок координат учитывается по сервису (Яндекс и 2ГИС — долгота первой,
Google и Apple — широта). Перевёрнутая пара дополнительно чинится в `sane()`.

---

## Файлы проекта
- `Код.gs` — весь Apps Script (вебхук Битрикса в нём НЕ хранится)
- `index.html` — карта для GitHub Pages
- `КОНТЕКСТ_ПРОЕКТА.md` — этот документ

Репозиторий: https://github.com/dmschrkshn/truck-map (публичный).
После каждого изменения обновлять этот документ и пушить вместе с кодом.
